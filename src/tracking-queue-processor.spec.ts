import { TrackingQueueProcessor } from './tracking-queue-processor';

describe('TrackingQueueProcessor', () => {
  it('should be defined', () => {
    expect(new TrackingQueueProcessor()).toBeDefined();
  });
});
